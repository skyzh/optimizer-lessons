# A Memo Merge Is a Transaction

The executable API and causal tests are frozen first. The learner walkthrough
will be written against that exact surface.

```console
cargo run -p optimizer-blog-lesson-3
cargo test -p optimizer-blog-lesson-3
```
